const VIDEO_EL = document.getElementById("camera-video");
const CANVAS_EL = document.getElementById("camera-video-canvas");
const IMAGE_EL = document.getElementById("camera-image");

const CAPTURE_BTN = document.getElementById("capture-btn");
const CAPTURE_AGAIN_BTN = document.getElementById("capture-again-btn");

module.exports = {
  VIDEO_EL,
  CANVAS_EL,
  IMAGE_EL,
  CAPTURE_BTN,
  CAPTURE_AGAIN_BTN,
};
